const BUTTON_BASE = "flex items-center gap-2 px-6 py-3 font-semibold transition-all duration-200 text-sm";
const BUTTON_PRIMARY = `${BUTTON_BASE} bg-primary text-white rounded hover:bg-blue-500 hover:scale-110`;
const BUTTON_OUTLINE = `${BUTTON_BASE} border border-border text-foreground rounded hover:border-primary hover:text-primary`;

export const BUTTON = {
  BUTTON_BASE,
  BUTTON_PRIMARY,
  BUTTON_OUTLINE
}