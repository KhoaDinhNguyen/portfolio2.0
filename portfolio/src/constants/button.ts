const BUTTON_BASE = "flex items-center gap-2 px-6 py-3 font-semibold rounded transition-all duration-200 text-sm w-full w-full sm:w-auto justify-center";
const BUTTON_PRIMARY = `${BUTTON_BASE} bg-primary text-white hover:bg-blue-500`;
const BUTTON_OUTLINE = `${BUTTON_BASE} border border-border text-foreground hover:border-primary hover:text-primary`;

export const BUTTON = {
  BUTTON_BASE,
  BUTTON_PRIMARY,
  BUTTON_OUTLINE
}